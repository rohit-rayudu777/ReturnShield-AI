# Docker build environment — nginx proxy handles routing to backend:8000
# Empty string means all API calls use relative paths (/health, /predict)
# which nginx.conf proxies to http://backend:8000
VITE_API_URL=
